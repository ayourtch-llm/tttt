[package]
name = "tttt-sandbox"
version = "0.1.0"
edition = "2021"

[dependencies]
thiserror = { workspace = true }
nix = { workspace = true }

[target.'cfg(target_os = "linux")'.dependencies]
landlock = "0.4"
